def func_title(value):
    """convert the first letters of each word to uppercase"""
    return value.title()


def new_func_title(value):
    """processed title function to incoming value"""
    return value.title()